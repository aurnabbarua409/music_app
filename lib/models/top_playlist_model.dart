class TopPlaylistModel {
  final String title, totalSongs, totalViews, image;
  TopPlaylistModel({
    required this.title,
    required this.totalSongs,
    required this.totalViews,
    required this.image,
  });
}
